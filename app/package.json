{
  "name": "app",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "npm run sync-data && node scripts/sync-fonts.mjs && vite",
    "build": "npm run sync-data && node scripts/sync-fonts.mjs && vite build",
    "preview": "vite preview",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "e2e": "npm run build && node scripts/prepare-e2e.mjs && playwright test",
    "e2e:docker": "docker run --rm -v \"$PWD/..\":/work -w /work --user \"$(id -u):$(id -g)\" -e HOME=/tmp --ipc=host mcr.microsoft.com/playwright:v$(node -p \"require('@playwright/test/package.json').version\")-noble npm -w app run e2e",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "svelte": "^5.19.0"
  },
  "devDependencies": {
    "@fontsource-variable/inter-tight": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@playwright/test": "^1.62.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/svelte": "^5.2.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^27.4.0",
    "svelte-check": "^4.1.0",
    "vite": "^6.1.0",
    "vitest": "^4.1.10"
  }
}
